spring:
  application:
    name: 'demo'
management:
  endpoint:
    env:
      show-values: always
    configprops:
      show-values: always
  endpoints:
    web:
      exposure:
        include: '*'